name: release

on:
  push:
    tags:
      - "v*"

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      id-token: write
    steps:
      - uses: actions/checkout@v5

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      # No registry-url: it writes an auth-token line into .npmrc, and npm only
      # reaches for OIDC when no token is configured.
      - uses: actions/setup-node@v5
        with:
          node-version: 22

      # Trusted publishing needs npm 11.5.1 or newer.
      - run: npm install -g npm@latest

      - run: bun install --frozen-lockfile

      - run: bun run check

      - run: bun run bundle

      # check and bundle already ran above, so skip prepublishOnly.
      - run: npm publish --ignore-scripts

      - run: gh release create "$GITHUB_REF_NAME" --generate-notes
        env:
          GH_TOKEN: ${{ github.token }}
